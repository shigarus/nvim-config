Don't forget install nerd font for a non-unicode symbols in iTerm2, or to patch existsing one with nerd-font.
For proto3 support - install cargo (probably via installing rust), and 'cargo install protols', and add cargo bin path to PATH
